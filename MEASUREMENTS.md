# 實測數據與方法

這份工具的判斷門檻與文案數字，全部來自實際的機器測量，不是估算或引用他人數據。
這一頁記錄那些測量的方法與結果，讓任何人都能判斷這些數字有多少參考價值、
以及在什麼條件下會不一樣。

- 測量日期：2026-08-18（主要測量）、2026-08-19（`llama3.2:1b` 基準）
- 測量對象：Ollama 0.32.14
- 測量環境：Windows 11 + WSL2（Ubuntu 26.04）+ 16 GB 級 NVIDIA 顯示卡
- 測試模型：`llama3.1:8b`（33 層）與 `llama3.2:1b`（17 層）
- **全部來自同一台機器。** 沒有第二台佐證，請把下面所有數字當成單點觀測而非統計分佈

---

## 方法

**為什麼要實測。** 「模型放不進顯示卡會慢多少」這件事，開始做之前有兩個互相矛盾的估計：
一個實務經驗值說「三成的層跑在 CPU 大約慢 20–40%」，一個純頻寬推算說「約 2 倍」。
差距大到不能兩個都寫進文案，所以決定量。

**怎麼量。** 用 Ollama 的 HTTP API 而不是互動模式，速度取自回應的
`eval_count ÷ eval_duration`（原始欄位，非自行計時）。

```bash
curl -s http://localhost:11434/api/generate -d '{
  "model":"llama3.1:8b",
  "prompt":"用三句話解釋什麼是光合作用",
  "stream":false,
  "options":{"num_gpu":24}
}'
```

三個檔位分別用 `num_gpu` 不設定 / 24 / 4 控制放進顯示卡的層數。

**兩條避免自我欺騙的規則：**

1. **同一個提示詞**，三檔位完全一致，長度穩定才能比較。
2. **每檔位跑兩次，一律取第二次。** 這條不是形式主義——全 GPU 檔位第一次只有
   47.71 tok/s、第二次 105.81，差一倍，因為第一次含模型載入暖機。
   如果取第一次當基準，算出來的減速倍率會被系統性放大。

**測量時的背景條件：** 機器上有一個穩定的背景影片解碼負載（約 2.8 GB 顯示卡記憶體），
全程未開關。這壓縮了可用容量，所以**絕對門檻**（例如溢位的臨界點）會偏早；
但三個檔位共用同一組背景條件，所以**相對倍率**是公平比較。

---

## 結果一：模型放不進顯示卡，會慢多少

| 放進顯示卡的層數 | 在 CPU 的比例 | 速度 | 相對全 GPU |
| --- | --- | --- | --- |
| 33 / 33 | 0% | 105.812 tok/s | 1.00× |
| 24 / 33 | 29% | 31.896 tok/s | **慢 3.32×** |
| 4 / 33 | 82% | 12.144 tok/s | **慢 8.71×** |

**結論：實務經驗值「20–40%」嚴重低估。** 三成的層被擠到 CPU 就慢 3.3 倍，
不是「稍微慢一點」。純頻寬推算的方向是對的——用同一個模型換算 29% 溢位得到約 3.6 倍，
與實測 3.32 倍相當接近。

工具的分級門檻（輕度 1–15%、中度 16–50%、重度 >50%）是**依這兩個實測點內插**的設計判斷，
不是量出來的。只有 29% 與 82% 兩個點有數據，中間與兩端是外推。

---

## 結果二：對話長度（context）的代價

| context | 載入後大小 | 執行位置 | KV cache | 速度 |
| --- | --- | --- | --- | --- |
| 4096（預設） | 5.3 GB | 全部在顯示卡 | 512 MiB | 105.812 tok/s |
| 65536 | 13 GB | 全部在顯示卡 | 8192 MiB | 88.455 tok/s |
| 98304 | 18 GB | 22% 在 CPU | 12288 MiB | 22.321 tok/s |

**兩個結論：**

1. **就算還放得進顯示卡，開大 context 也會變慢。** 65536 完全沒有溢位，速度仍掉了 16%。
   對話記憶越大，每產生一個字要掃過的資料就越多。
2. **放不下時就變成上面那張表的問題。** 98304 觸發溢位，速度掉到原本的 1/4.7。

**KV cache 的計算式被引擎自己的紀錄證實：**

```
每 token = 2 × 層數 × KV heads × head dim × 2 bytes
llama3.1:8b = 2 × 32 × 8 × 128 × 2 = 131072 bytes = 128 KiB/token
```

三個實測點完全線性：4096 → 512 MiB、65536 → 8192 MiB、98304 → 12288 MiB，
換算皆為 128 KiB/token。

**前提條件：** 這個換算只在 KV cache 未量化（f16）、未開 flash attention、
單一並行請求時成立。三者任一改變都會讓數字失效，所以工具會先讀這三個設定再套公式。

---

## 結果三：顯示卡記憶體的量測本身不可靠

同一天對閒置顯示卡記憶體取樣八次：`3016 / 2735 / 2725 / 2690 / 2659 / 2638 / 2799 / 1763` MiB，
**最大差距超過 1.2 GB**。

所以工具不用單次取樣，改為取樣三次取中位數並保留容差。
「模型佔用了多少」的推算也因此加上一個 +250 MiB 的固定修正
（三個檔位的實測殘差穩定落在 +223 ~ +280 MiB，來自 CUDA context 與非層權重的開銷）。

另外在對照三個來源之後發現：Windows 的 `GPU Adapter Memory` 效能計數器與 `nvidia-smi`
吻合到 0.1% 以內（15,709 MB vs 15,689 MiB）可以信任，但 `GPU Process Memory`
的單一程序數值會膨脹（前三名加總 24,452 MB，超過整張卡的實際用量），
只能拿來排名不能當絕對值。

---

## 基準值對照表（`--live` 模式使用）

`check.sh --live` 會實際跑一次模型並量出速度。要判斷那個速度是快是慢，需要一個
可比對的基準——而基準必須同時吻合**顯示卡**與**模型**才有意義：同一張卡上，
1B 模型本來就比 8B 快好幾倍，拿其中一個的數字去比另一個的基準會得出荒謬結論。

因此基準表以 (顯示卡, 模型) 為鍵。目前只有兩筆，都來自同一台機器：

| 顯示卡 | 模型 | 速度 | 層數 | 量測日期 | 背景 VRAM |
| --- | --- | --- | --- | --- | --- |
| NVIDIA GeForce RTX 5070 Ti | `llama3.1:8b` | 105.812 tok/s | 33/33 | 2026-08-18 | 2799 MiB（含影片解碼負載）|
| NVIDIA GeForce RTX 5070 Ti | `llama3.2:1b` | 246.802 tok/s | 17/17 | 2026-08-19 | 2695 MiB（無影片解碼負載）|

**兩次量測的條件差異，照實記錄：**

- 不同日期（相隔一天），期間機器重開過
- 8b 那次背景有持續的影片解碼負載；1b 那次沒有，背景是桌面程式與遊戲平台常駐
- 背景 VRAM 相近（2799 vs 2695 MiB），但 1b 那次的取樣三次完全相同（2695/2695/2695），
  8b 那次則在漂移中
- **兩次的條件不完全相同，所以這兩個數字之間不可互相推算**，
  它們各自只對「同款卡 + 同一個模型」有參考價值

**`llama3.2:1b` 的細節：**

- 第 1 次 179.972 tok/s、第 2 次 **246.802 tok/s** —— 暖機效應讓第一次慢了 37%，
  再次印證「兩次取第二次」不是形式主義
- 載入後 1.5 GB、17 層全部在顯示卡、context 4096
- KV cache 128 MiB / 4096 tokens = **32 KiB/token**（8b 是 128 KiB/token，
  因為層數與 KV head 數都少）

**這兩筆基準的效力邊界：** 每一筆都是**單一機器、單次測量**。它們可以用來回答
「你的速度跟同款卡上量到的數字是不是同一個量級」，但**不足以構成「正常範圍」**——
要講範圍需要多台機器的分佈，我們沒有。`--live` 的措辭因此是「本專案實測到 X，
你的是 Y」而不是「你在正常範圍內」。顯示卡或模型任一不吻合時，只報數字不評價。

---

## 這些數字在你的機器上會不一樣

- **卡的容量不同** → context 的溢位臨界點就不同。98304 這個數字是 16 GB 級卡的結果，
  不是通用值。
- **模型不同** → 層數與 KV cache 大小都不同，`128 KiB/token` 只適用於
  `llama3.1:8b` 這類架構。
- **背景負載不同** → 可用容量不同，溢位會提早或延後發生。
- **減速倍率**（3.32× / 8.71×）預期比較穩定，因為它反映的是顯示卡記憶體與系統記憶體的
  頻寬差距，那是硬體特性。但這一點**只在一台機器上驗證過**，沒有第二台佐證。

---

## 沒有量到的部分

誠實列出，避免這一頁看起來比實際更完整：

- 三檔位倍率只在一台機器、一個模型上量過
- 分級門檻的中間與兩端是內插外推，不是實測
- 無背景負載時的倍率沒有量過
- 「完全跑在 CPU」的情境（工具的 R2 規則）尚未實測，目前只有理論值
- 這些測量沒有重複多日，無法排除當天的偶發因素

原始輸出（未經修改的指令與回應）保留在開發者本機，未隨專案發布。
